# SDR Cockpit Frontend Configuration

# API Mode: "offline" (demo mode with mock data) or "online" (connect to backend)
VITE_API_MODE=online

# Backend API URL (only used in online mode)
VITE_API_BASE_URL=http://localhost:8000

# Enable plot sandbox for development (optional)
# VITE_PLOT_SANDBOX=true
