name: CI/CD
# GitHub Actions CI/CD Pipeline
#
# Note: This workflow uses Docker buildx for multi-arch builds, which is the most
# efficient approach for CI (parallel builds, GitHub cache integration). For local
# development with Podman, use scripts/build-prod.sh which supports both runtimes.

on:
  pull_request:
    branches:
      - trunk
  push:
    branches:
      - trunk

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  frontend-checks:
    name: Frontend Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Run frontend checks
        run: ./scripts/check-frontend.sh

  backend-checks:
    name: Backend Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd backend
          uv sync --frozen

      - name: Run backend checks
        run: ./scripts/check-backend.sh

  simulator-checks:
    name: Simulator Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "simulator/uv.lock"

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd simulator
          uv sync --frozen

      - name: Run simulator checks
        run: ./scripts/check-simulator.sh

  generate-api-docs:
    name: Generate API Documentation
    needs: [frontend-checks, backend-checks]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Generate OpenAPI HTML documentation
        run: ./scripts/generate-docs.sh

      - name: Upload API documentation artifact
        uses: actions/upload-artifact@v7
        with:
          name: api-documentation
          path: docs/api/index.html
          retention-days: 90

  deploy-docs:
    name: Deploy API Docs to GitHub Pages
    needs: [generate-api-docs]
    if: github.ref == 'refs/heads/trunk' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Generate OpenAPI HTML documentation
        run: ./scripts/generate-docs.sh

      - name: Prepare Pages directory
        run: |
          mkdir -p _site/api
          cp docs/api/index.html _site/api/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  integration-tests:
    name: Integration Tests
    needs: [frontend-checks, backend-checks, simulator-checks]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build all images
        run: |
          docker compose -f docker-compose.ci.yml build

      - name: Start services
        run: |
          docker compose -f docker-compose.ci.yml up -d

      - name: Wait for services to be healthy
        run: |
          echo "Waiting for services to start..."
          sleep 10

      - name: Check service health
        run: |
          echo "Checking if all containers are running..."
          docker compose -f docker-compose.ci.yml ps

          RUNNING=$(docker compose -f docker-compose.ci.yml ps --services --filter "status=running" | wc -l)
          EXPECTED=3  # backend, simulator, redis

          if [ "$RUNNING" -ne "$EXPECTED" ]; then
            echo "Error: Expected $EXPECTED containers running, but found $RUNNING"
            docker compose -f docker-compose.ci.yml logs
            exit 1
          fi

          echo "All containers are running successfully!"

      - name: Test backend API health endpoint
        run: |
          sleep 5

          for i in {1..10}; do
            if curl -f http://localhost:8000/api/healthcheck; then
              echo "Backend API health check passed!"
              exit 0
            fi
            echo "Attempt $i failed, retrying..."
            sleep 2
          done

          echo "Backend health check failed after 10 attempts"
          docker compose -f docker-compose.ci.yml logs backend
          exit 1

      - name: Test frontend is being served by backend
        run: |
          for i in {1..10}; do
            if curl -f http://localhost:8000/ | grep -q "<!doctype html>"; then
              echo "Frontend is being served successfully by backend!"
              exit 0
            fi
            echo "Attempt $i failed, retrying..."
            sleep 2
          done

          echo "Frontend check failed after 10 attempts"
          docker compose -f docker-compose.ci.yml logs backend
          exit 1

      - name: Collect logs on failure
        if: failure()
        run: |
          echo "=== Collecting container logs ==="
          docker compose -f docker-compose.ci.yml logs

      - name: Stop services
        if: always()
        run: |
          docker compose -f docker-compose.ci.yml down -v

  build-and-push-app:
    name: Build and Push App Image (Multi-Arch)
    needs: [frontend-checks, backend-checks, simulator-checks, integration-tests]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up QEMU for multi-arch builds
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.ref == 'refs/heads/trunk'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push multi-arch image
        if: github.ref == 'refs/heads/trunk'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/sdr-cockpit.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build image (PR validation only)
        if: github.ref != 'refs/heads/trunk'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/sdr-cockpit.Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: sdr-cockpit:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-and-push-simulator:
    name: Build and Push Simulator Image (Multi-Arch)
    needs: [frontend-checks, backend-checks, simulator-checks, integration-tests]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up QEMU for multi-arch builds
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.ref == 'refs/heads/trunk'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-simulator
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push multi-arch image
        if: github.ref == 'refs/heads/trunk'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/simulator.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build image (PR validation only)
        if: github.ref != 'refs/heads/trunk'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/simulator.Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: sdr-cockpit-simulator:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-rpm:
    name: Build RPM Package
    needs: [frontend-checks, backend-checks, simulator-checks, integration-tests]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build x86_64 image for RPM
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/sdr-cockpit.Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: sdr-cockpit:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Export image as tar for RPM
        run: |
          docker save sdr-cockpit:latest -o sdr-cockpit-image.tar

      - name: Build RPM in AlmaLinux container
        run: |
          docker run --rm \
            -v "$PWD":/workspace:Z \
            -w /workspace \
            almalinux:9 \
            bash -c "
              dnf install -y rpm-build
              ./scripts/build-rpm-from-tar.sh sdr-cockpit-image.tar 0.1.0 1 x86_64
            "

      - name: Verify RPM contents
        run: |
          echo "=== RPM Package Info ==="
          rpm -qip sdr-cockpit*.rpm
          echo ""
          echo "=== RPM File List ==="
          rpm -qlp sdr-cockpit*.rpm

      - name: Upload RPM artifact
        if: github.ref == 'refs/heads/trunk'
        uses: actions/upload-artifact@v7
        with:
          name: sdr-cockpit-rpm
          path: sdr-cockpit*.rpm
          retention-days: 90
