[project]
name = "sdr-cockpit-simulator"
version = "0.1.0"
description = "SDR Cockpit Mock Simulator"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
