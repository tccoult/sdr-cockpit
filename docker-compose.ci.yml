version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - simulator
    networks:
      - sdr-network
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379

  simulator:
    build:
      context: .
      dockerfile: docker/simulator.Dockerfile
    environment:
      - SDR_MODE=test
    networks:
      - sdr-network

  redis:
    image: redis:7-alpine
    networks:
      - sdr-network

networks:
  sdr-network:
    driver: bridge
